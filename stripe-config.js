/**
 * Stripe Payment Link (no backend required)
 *
 * 1. Stripe Dashboard → Product catalog → create product & price (or use existing).
 * 2. Payment Links → New → pick the price → create link.
 * 3. Copy the full URL (starts with https://buy.stripe.com/...) and paste below.
 *
 * Test mode links work on localhost; switch to live mode when you go live.
 */
window.BYMELTY_STRIPE_PAYMENT_LINK = "https://buy.stripe.com/9B6aEYc9rb0Y0DsdY2dfG05";
